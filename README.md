It is necessary to write a macro ADDTHREE that takes three parameters as input (unsigned numbers) and stores the sum of all three in the location of the first parameter.
The macro must work correctly for variables of size Double Word, Word and Byte.
If the macro parameters are of different sizes, an error should be generated
